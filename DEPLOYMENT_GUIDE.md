# دليل النشر الكامل | Complete Deployment Guide

## 📋 الخطوات التالية للنشر

### 1️⃣ إعداد Git Repository

```bash
# تهيئة Git (إذا لم يكن مهيئاً)
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit: Syrian Exchange Rates website"

# إنشاء repository على GitHub ثم إضافة الـ remote
git remote add origin https://github.com/YOUR_USERNAME/syrian-exchange-rates.git

# رفع الكود
git branch -M main
git push -u origin main
```

### 2️⃣ تفعيل GitHub Actions

بعد رفع الكود على GitHub:

1. ✅ سيتم تفعيل GitHub Actions تلقائياً
2. ✅ سيتم تحديث البيانات مرتين يومياً (9 صباحاً و9 مساءً توقيت سوريا)
3. ✅ يمكنك تشغيل التحديث يدوياً من **Actions** > **Update Exchange Rates Data** > **Run workflow**

### 3️⃣ النشر على Cloudflare Pages

#### الطريقة الأسهل (من GitHub):

1. **سجّل دخولك إلى** [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. اذهب إلى **Pages**
3. اضغط **Create a project**
4. اختر **Connect to Git**
5. اربط حساب GitHub الخاص بك (إذا لم يكن مربطاً)
6. اختر المستودع **syrian-exchange-rates**
7. في إعدادات البناء:
   - **Project name**: `syrian-exchange-rates`
   - **Framework preset**: None
   - **Build command**: (اتركه فارغاً)
   - **Build output directory**: `/`
8. اضغط **Save and Deploy**
9. انتظر حتى ينتهي النشر (بضع ثوانٍ)
10. 🎉 موقعك جاهز!

#### رابط موقعك سيكون:
```
https://syrian-exchange-rates.pages.dev
```

### 4️⃣ تخصيص النطاق (Custom Domain) - اختياري

1. في Cloudflare Pages dashboard
2. اختر **Custom domains**
3. اضغط **Set up a custom domain**
4. أدخل النطاق الخاص بك (مثل: `exchange.sy`)
5. اتبع الخطوات لإضافة سجلات DNS

### 5️⃣ تحديث البيانات يدوياً (للاختبار)

**من GitHub:**
1. اذهب إلى **Actions** tab
2. اختر **Update Exchange Rates Data**
3. اضغط **Run workflow**
4. اختر الفرع: `main`
5. اضغط **Run workflow** مرة أخرى

**من الكمبيوتر:**
```bash
# تثبيت التبعيات
npm install

# تشغيل script جلب البيانات
npm run fetch

# رفع التحديثات
git add data/
git commit -m "Update data"
git push
```

---

## 🔧 حل المشاكل الشائعة

### المشكلة: البيانات لا تتحدث

**الحل:**
1. تحقق من أن GitHub Actions يعمل في **Actions** tab
2. تأكد أن ملفات JSON في مجلد `data/` موجودة
3. تفقد logs في GitHub Actions لمزيد من التفاصيل

### المشكلة: الموقع يظهر بيانات قديمة

**الحل:**
1. امسح cache المتصفح (Ctrl+Shift+Delete)
2. أعد تحميل الصفحة (F5)
3. أو اضغط Ctrl+F5 للتحديث القسري

### المشكلة: GitHub Actions يفشل

**الحل:**
1. تأكد من أن `package.json` يحتوي على التبعيات الصحيحة
2. تحقق من أن script `fetch-data.js` يعمل محلياً: `npm run fetch`
3. افحص logs الخطأ في Actions

### المشكلة: التصميم لا يظهر بشكل صحيح

**الحل:**
1. تأكد أن ملف `css/style.css` موجود
2. تحقق من أن الرابط في `<head>` صحيح: `<link rel="stylesheet" href="css/style.css">`
3. امسح cache المتصفح

---

## 📊 مصادر البيانات

البيانات تُجلب من:
- أسعار العملات: https://sp-today.com/currencies
- أسعار الذهب: https://sp-today.com/gold
- أسعار العملات الرقمية: https://sp-today.com/crypto

---

## 🎨 تخصيص التصميم

### تغيير الألوان
عدّل ملف `css/style.css` في قسم `:root`:

```css
:root {
  --bg-primary: #f8f9fa;        /* لون الخلفية الرئيسي */
  --accent-primary: #2563eb;     /* اللون الأساسي */
  --accent-gold: #d97706;        /* لون الذهب */
  /* ... */
}
```

### تغيير أوقات التحديث
عدّل ملف `.github/workflows/update-data.yml`:

```yaml
schedule:
  - cron: '0 6,18 * * *'  # 6:00 و18:00 UTC = 9:00 و21:00 سوريا
```

### إضافة عملات جديدة
عدّل ملفات JSON في مجلد `data/`:

```json
// data/currencies.json
{
  "rates": [
    {
      "name": "اسم العملة",
      "buy": 0,
      "sell": 0
    }
  ]
}
```

---

## 🔒 الأمان والخصوصية

الموقع يستخدم:
- ✅ HTTPS آمن (من Cloudflare)
- ✅ Cache-Control Headers
- ✅ Security Headers
- ✅ لا توجد بيانات شخصية تُجمع
- ✅ لا توجد cookies

---

## 📈 المراقبة والتحليل

يمكنك إضافة Google Analytics أو أي خدمة تحليلات أخرى:

1. أضف كود التتبع في `index.html` قبل `</head>`
2. أو استخدم Cloudflare Web Analytics (مجاني وسهل)

---

## 🎯 التحقق من نجاح النشر

بعد النشر، تأكد من:

- [ ] الموقع يفتح بدون أخطاء
- [ ] البيانات تُعرض بشكل صحيح
- [ ] Dark/Light mode يعمل
- [ ] الموقع متجاوب على الموبايل
- [ ] وقت آخر تحديث يظهر
- [ ] GitHub Actions يعمل
- [ ] البيانات تُحدث تلقائياً

---

## 💡 نصائح إضافية

1. **احتفظ بنسخة احتياطية**: احتفظ بنسخة من الملفات قبل التعديل
2. **اختبر محلياً**: جرب أي تغييرات محلياً قبل النشر
3. **استخدم GitHub Desktop**: إذا كنت تفضل واجهة رسومية
4. **فعّل Notifications**: في GitHub للحصول على تنبيهات عن التحديثات
5. **راقب المستخدمين**: استخدم Analytics لفهم كيف يستخدم الناس الموقع

---

## 🆘 الحصول على المساعدة

إذا واجهت أي مشكلة:

1. افحص **Console** في المتصفح (F12)
2. راجع logs في GitHub Actions
3. اقرأ ملف `README.md` بالكامل
4. ابحث عن المشكلة في Stack Overflow
5. افتح Issue في GitHub

---

## 📞 التواصل

للأسئلة والاقتراحات:
- GitHub Issues
- Email: support@syrian-exchange-rates.com

---

**تم إنشاء هذا الدليل لمساعدتك في نشر موقعك بنجاح! 🚀**
